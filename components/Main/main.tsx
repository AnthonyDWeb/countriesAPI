import React, { useState } from "react";
import Galery from "./galery";

export const Main = () => {
	return (
		<main>
			<h1 className="main-title">Countries API</h1>
			<Galery />
		</main>
	);
};
